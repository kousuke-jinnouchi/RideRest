<div class="container my-5">
  <div class="row">
    <div class="col-md-10 mx-auto">
      <div class="mb-4 pb-2 border-bottom">
        <h2>
          <i class="fas fa-search text-info"></i>
          「<span class="font-weight-bold"><%= @content %></span>」の検索結果
        </h2>
      </div>
      <% if @results.any? %>
        <p class="text-muted mb-4"><%= @results.count %>件の駐輪場が見つかりました。</p>
        <div class="list-group shadow-sm">
          <% @results.each do |parking_lot| %>
            <%= link_to parking_lot_path(parking_lot), class: "list-group-item list-group-item-action flex-column align-items-start" do %>
              <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1 text-primary"><%= parking_lot.parking_lot_name %></h5>
                <small class="text-muted"><%= parking_lot.created_at.strftime('%Y/%m/%d') %> 投稿</small>
              </div>
              <p class="mb-1 text-secondary">
                <i class="fas fa-map-marker-alt fa-fw"></i>
                <%= parking_lot.address %>
              </p>
              <div class="mt-2">
                <% parking_lot.tag_list.each do |tag| %>
                  <span class="badge badge-info mr-1"><%= tag %></span>
                <% end %>
              </div>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div class="text-center p-5">
          <p class="h5 text-muted">該当する駐輪場は見つかりませんでした。</p>
          <p class="text-muted">検索キーワードを変えて、再度お試しください。</p>
          <%= link_to "駐輪場一覧に戻る", parking_lots_path, class: "btn btn-outline-secondary mt-3" %>
        </div>        
      <% end %>
    </div>
  </div>
</div>
